# https://docs.cocogitto.io/reference/config.html

tag_prefix = "v"

from_latest_tag = true
branch_whitelist = ["main", "master"]
ignore_merge_commits = true

[git_hooks]
# 支持在 commit-msg 阶段自动校验 Conventional Commits
commit-msg = "cog check --hook"

[changelog]
path = "CHANGELOG.md"
template = "default"  
