# Git - Great tool!

Make everything tracable

```
git helpers
Git help
git help -a
git help -g
git help everyday
GIT_TRACE=1 GIT_CURL_VERBOSE=1 git pull
```

```
# get branch name
git rev-parse --abbrev-ref HEAD
```

