# https://taskfile.dev/docs/guide
# https://taskfile.dev/reference/templating/
version: '3'
tasks:
  config: mise config ls

  # not works?
  greet: mise run greet --user jdx -g "hey" "How are you?"
  greet.h: mise run greet --help

  env: echo $test_env
