# https://taskfile.dev/docs/guide
# https://taskfile.dev/reference/templating/
version: '3'
tasks:
  hack: |
    ./install-hack.sh
    tmp-unpack/*/install --help
    
  get.install.script: |
    curl -L https://nixos.org/nix/install > install.sh

  